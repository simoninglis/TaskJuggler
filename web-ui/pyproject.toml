[tool.poetry]
name = "taskjuggler-webui"
version = "0.1.0"
description = "Web UI for TaskJuggler"
authors = ["TaskJuggler Development Team"]

[tool.poetry.dependencies]
python = "^3.9"
websockets = "^12.0"
watchdog = "^4.0.0"

[tool.poetry.dev-dependencies]

[tool.poetry.group.dev.dependencies]
playwright = "^1.52.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"